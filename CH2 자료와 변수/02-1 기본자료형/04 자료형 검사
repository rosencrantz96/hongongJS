자료형 검사: typeof연산자 (얘도 피연산자를 하나만 갖는 단항연산자)
typeof(자료) 

typeof('문자열')
"string"

typeof(273)
"number"

typeof(true)
"boolean"

- 연산자 뒤에 괄호가 없어도 상관 없음 

> typeof 10 === "number"
True



*템플릿 문자열: 백틱(`)기호로 감싸 만든다. 문자열 내부에 `${...}` 기호를 사용하여 표현식을 넣으면 표현식이 문자열 안에서 계산됨 

> console.log(`표현식 273 + 52의 값은 ${273 + 52}입니다...!`)
표현식 273 + 52의 값은 325입니다...!

* ===, !== : 값과 자료형이 같은지를 비교하는 연산자
* ==, !== : 값이 같은지를 비교하는 연산자 